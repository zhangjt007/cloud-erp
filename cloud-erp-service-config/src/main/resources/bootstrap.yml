spring:
  application:
    name: cloud-erp-service-config
  cloud:
    config:
      server:
        git:
          uri:
server:
  port: 8763

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
