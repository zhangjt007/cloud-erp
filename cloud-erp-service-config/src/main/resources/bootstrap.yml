spring:
  application:
    name: cloud-erp-service-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhangjt007/cloud-erp
          username: zhangjt007@gmail.com
          password: Github.pwd=2.5
          search-paths: config-repo
server:
  port: 8763